/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  compiler: {
    styledComponents: true,
  },
  // output: "export",
  trailingSlash: true,
};

module.exports = nextConfig;
